#include<iostream>
using namespace std;
int main(){
    int a, b;
    cout << "Enter the value of a: ";
    cin >> a;
    cout << "Enter the value of b: ";
    cin >> b;

    // swap(a, b);  using built in function that's "swap"
    
    // int temp;
    // temp = a;
    // a = b;          // using temp variable
    // b = temp;

    cout << "Swaping executed succesfully!" << endl;
    cout << "a is: " << a << endl;
    cout << "b is: " << temp << endl;

}